
import { buildCss } from 'dmx-frontend/helpers/build-css';
import { module, test } from 'qunit';

module('Unit | Helper | build css');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = buildCss([42]);
  assert.ok(result);
});

